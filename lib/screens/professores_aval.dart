import 'package:flutter/material.dart';

class Professores_aval extends StatelessWidget {
  const Professores_aval({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
